## Notice to all contributors:

The toolkit packages are moving to the collab-npm repository, and will be
published to the `@collabui` namespace.

If you are seeing this message AND you have a live PR open currently, please
ensure that your changeset uses the updated namespace:

e.g. update `require(...)` statements
```
require('collab-ui-ng')            // <- old
require('@collabui/collab-ui-ng')  // <- new
```

e.g. update `@import ...` statements
```
@import '~collab-ui/scss/...'            // <- old
@import '~@collabui/collab-ui/scss/...'  // <- new
```
