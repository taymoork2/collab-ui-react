'use strict';

angular.module('WebexClientVersions').service('WebexClientVersion', [
  '$q',
  '$log',
  '$translate',
  '$filter',
  'Authinfo',
  'WebExUtilsFact',
  'Notification',
  function (
    $q,
    $log,
    $translate,
    $filter,
    Authinfo,
    WebExUtilsFact,
    Notification
  ) {

  }
]);
