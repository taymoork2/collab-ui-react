export enum CertificateCheck {
  SIGNING_AUTHN = 'SIGNING_AUTHN',
  NONE = 'NONE',
  NEITHER = 'NEITHER',
}

export enum CertificateType {
  MULTIPLE = 'MULTIPLE',
  SINGLE = 'SINGLE',
  NEITHER = 'NEITHER',
}
