'use strict';

describe('Directive: scheduleUpgradeConfiguration', function () {
  beforeEach(module('Hercules'));
  // TODO: should check that the service-type attribute of this directive
  // is only one-way bound. I tried to make it work for hours, I gave up
  // — Thomas
});
