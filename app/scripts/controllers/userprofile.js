'use strict';

angular.module('wx2AdminWebClientApp')
	.controller('UserProfileCtrl', ['$scope', '$location', 'Log', 'Utils', '$filter',
		function($scope, $location, Log, Utils, $filter) {


		}
	]);